import React, { useEffect, useState } from "react";
import { Button } from "react-bootstrap";
import PostForm from "./PostForm";
function postCard(props) {
  return <post></post>;
}

export default function Wall(props) {
  return (
<> <PostForm name="test" text = " Just a text"/>
    <PostForm name="test2" text = "Listening on port 5001Successfully connected to MongoDB AtlasListening on port 5001Successfully connected to MongoDB Atlas!Listening on port 5001Successfully connected to MongoDB AtlasListening on port5001nfdgnuccessfully connected to MongoDB Atlas "  />
</>
   
  );
}
